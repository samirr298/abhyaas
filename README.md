FlaskApp-OOP

Starter structure created. To run locally:

```powershell
py -3 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install flask
python run.py
```

Don't commit yet — follow your git workflow.
